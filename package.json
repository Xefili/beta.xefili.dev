{
  "dependencies": {
    "autoprefixer": "^10.4.20",
    "tailwindcss": "^3.4.12"
  },
  "scripts": {
    "devExtra": "npx tailwindcss -i dashboard/in.css -o dashboard/extra.css --watch",
    "dev": "npx tailwindcss -i styles/in.css -o styles/app.css --watch",
    "build": "npx tailwindcss -i styles/in.css -o styles/app.css --minify"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prettier-plugin-tailwindcss": "^0.6.8"
  }
}

{
  "dependencies": {
    "autoprefixer": "^10.4.20",
    "tailwindcss": "^4.0.0"
  },
  "scripts": {
    "devExtra": "npx @tailwindcss/cli -i dashboard/in.css -o dashboard/extra.css --watch",
    "dev": "npx @tailwindcss/cli -i styles/in.css -o styles/app.css --watch",
    "build": "npx @tailwindcss/cli -i styles/in.css -o styles/app.css --minify"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prettier-plugin-tailwindcss": "^0.6.8"
  }
}
